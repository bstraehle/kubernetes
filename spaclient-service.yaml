apiVersion: v1
kind: Service
metadata:
  name: spaclient-service
  labels:
    name: spaclient-service
    app: spaclient-app
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 80
  selector:
    name: spaclient-app-pod
    app: spaclient-app