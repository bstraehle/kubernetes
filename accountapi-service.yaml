apiVersion: v1
kind: Service
metadata:
  name: accountapi-service
  labels:
    name: accountapi-service
    app: accountapi-app
spec:
  type: ClusterIP
  ports:
    - port: 8002
      targetPort: 80
  selector:
    name: accountapi-app-pod
    app: accountapi-app